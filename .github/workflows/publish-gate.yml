name: publish-gate

# Re-runs the anti-leak gate on every push and pull request.
#
# Mode is `ci`: the generic pattern layer only. The private layer lives outside this
# repository by design — a gate that shipped its own blocklist would publish exactly what it
# exists to hide. Full coverage runs on the authoring side before anything is pushed here.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install gitleaks
        run: |
          curl -sSL https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/gitleaks_8.30.1_linux_x64.tar.gz \
            | tar -xz -C /usr/local/bin gitleaks
          gitleaks version

      - name: Run publish gate
        env:
          PUBLISH_GATE_MODE: ci
        run: bash scripts/publish-gate.sh "$GITHUB_WORKSPACE"

      - name: Verify the exit contract (the framework's own regression suite)
        run: |
          # The runner's default shell is `bash -e`; a fixture returning exit 5 is the
          # CORRECT contract behaviour, not a failure — disable errexit and manage the
          # verdict explicitly.
          set -u +e
          fail=0
          check() {
            bash scripts/verify-gate.sh ".claude/sdd/fixtures/$1" >/dev/null 2>&1
            got=$?
            if [ "$got" = "$2" ]; then
              echo "  ok  $1 -> exit $got"
            else
              echo "  FAIL $1 -> exit $got (expected $2)"; fail=1
            fi
          }
          check DEFINE_FIXTURE_NEEDS_CLARIFICATION.md 5
          check DEFINE_FIXTURE_CLARIFY_RESOLVED.md 0
          check DEFINE_FIXTURE_TOKEN_IN_FENCE.md 0
          check DEFINE_FIXTURE_CONTROL.md 0
          exit $fail

      - name: Shell syntax
        run: for f in scripts/*.sh; do bash -n "$f" || exit 1; done
